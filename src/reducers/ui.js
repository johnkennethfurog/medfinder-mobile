// import {MENU_HOME, SORT_BY_NEWEST, MY_TRUCKS} from '../utils/constants';

const INITIAL_STATE = {};

export default (state = INITIAL_STATE, action = {}) => {
  switch (action.type) {
    /*    case SEARCH_DEALS_FILTER: {
      return {
        ...state,
        searchDealsText: action.payload,
      };
    }*/
    default:
      return state;
  }
};
